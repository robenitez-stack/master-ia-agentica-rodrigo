# 02 — Red neuronal desde cero con PyTorch

Estado: `FULL_RUN_PENDING`

El modo `fast` es un smoke test local no publicable. Los resultados definitivos se incorporarán tras ejecutar y validar el modo `full`.
