# 01 — Neurona manual NumPy sobre MNIST

Estado: `FULL_RUN_PENDING`
Resultados publicables: no disponibles todavía.

El modo `fast` se utiliza únicamente como smoke test local. Las métricas definitivas se incorporarán después de una ejecución `full` validada.
